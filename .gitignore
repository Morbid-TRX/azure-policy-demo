# Local Terraform state — contains sensitive data, never push this
.terraform/
terraform.tfstate
terraform.tfstate*
.terraform.lock.hcl

# Environment variable files — credentials stay local
*.env
.env